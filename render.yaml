services:
  - type: web
    name: wifi-check-backend
    env: node
    plan: free
    buildCommand: npm install
    startCommand: node backend.js
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000

